version: "3.9"

services:

  backend:
    build: ./backend/server
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production

  ai-service:
    build: ./backend/ai-services
    ports:
      - "8000:8000"

  admin:
    build: ./admin
    ports:
      - "3001:80"